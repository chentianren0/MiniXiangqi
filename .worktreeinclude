# Copied intact into every new agent worktree by its setup workflow;
# seed-worktree.sh is the same workflow when the harness does not provide one
# (literal gitignored paths only). The checker is read-only, so every item here
# must arrive with the worktree or be produced by build-core-xcframework.sh.
# Re-run that generator after any input changes.
Generated/
# Resources is a file-system-synchronized group enumerated when Xcode plans the
# build: a copy appearing during a build lands only in the next one. Seeding the
# engine asset pack makes the first build complete.
MiniXiangqi/Resources/engine-assets.mxqpack
