# Claude Code Notes

The binding rules for this repository are in AGENTS.md; follow them in full:

@AGENTS.md

Quick orientation on top of those rules:

- This is a design-first repository: `docs/` holds the product, interaction, rules, architecture, data, engine, and testing contracts, and the code is still the generated Xcode scaffold. Each document's status line states what is accepted versus draft; read the relevant contract before proposing or making a change.
- Open product and engineering questions live in each document's **Need to discuss** section and in GitHub Issues; progress and decisions-in-flight belong there, not in the documents.
- Remote Git and GitHub operations require the isolated identity environment: `source .git/minixiangqi-control/activate.zsh` first, and operate only on `ppppvz`-owned repositories.
